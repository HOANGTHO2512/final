# 專案設置指南

歡迎使用本專案！本指南將幫助您在從 GitHub 克隆後快速啟動專案。

## 📋 系統需求

在開始之前，請確保您的電腦上已安裝以下軟體：

- **Node.js** (版本 14 或更高) - [下載](https://nodejs.org/)
- **npm** (通常隨 Node.js 安裝) 或 **yarn** 或 **pnpm**
- **Git** (用於版本控制)

## 🚀 快速開始

### 步驟 1: 克隆專案

```bash
git clone https://github.com/HOANGTHO2512/final.git
cd final-project
```

### 步驟 2: 安裝依賴套件

使用 npm 安裝所有必需的依賴項：

```bash
npm install
```

**或者，如果您使用 yarn：**

```bash
yarn install
```

**或者，如果您使用 pnpm：**

```bash
pnpm install
```

### 步驟 3: 啟動開發伺服器

執行以下命令啟動本地開發伺服器：

```bash
npm run dev
```

開發伺服器通常會在 `http://localhost:5173` 上執行。在瀏覽器中打開此網址即可查看應用程式。

> 💡 **提示**：開發伺服器支援熱重載，當您修改程式碼時，頁面會自動刷新。

## 📦 可用的命令

| 命令 | 說明 |
|------|------|
| `npm run dev` | 啟動開發伺服器 (含熱重載) |
| `npm run build` | 為生產環境建置應用程式 |
| `npm run preview` | 預覽生產建置版本 |
| `npm run lint` | 檢查程式碼品質 |

## 🏗️ 專案結構

```
final-project/
├── src/
│   ├── assets/              # 靜態資源檔案
│   ├── components/          # React 元件
│   │   ├── AuthModal.jsx
│   │   ├── ChatBot.jsx
│   │   ├── Navbar.jsx
│   │   └── ...
│   ├── context/             # React Context (狀態管理)
│   │   └── AuthContext.jsx
│   ├── data/                # 資料檔案
│   │   ├── careerData.js
│   │   └── quizData.js
│   ├── pages/               # 頁面元件
│   │   ├── HomePage.jsx
│   │   ├── CareerFitPage.jsx
│   │   └── ...
│   ├── router/              # 路由配置
│   │   └── index.jsx
│   ├── App.jsx              # 主應用程式元件
│   ├── main.jsx             # 應用程式入口
│   └── index.css            # 全域樣式
├── public/                  # 公開資源檔案
├── package.json             # 專案配置和依賴項
├── vite.config.js           # Vite 配置
├── tailwind.config.js       # Tailwind CSS 配置
├── eslint.config.js         # ESLint 配置
└── README.md                # 專案說明
```

## 🛠️ 技術堆疊

本專案使用以下技術：

- **React 19** - 使用者介面框架
- **Vite** - 快速建置工具
- **Tailwind CSS** - 工具優先的 CSS 框架
- **React Router** - 客戶端路由
- **Chart.js** - 資料視覺化
- **Google Generative AI** - AI 整合

## 🔧 環境變數 (如需要)

如果專案需要環境變數，請在根目錄建立 `.env` 檔案：

```
VITE_API_KEY=your_api_key_here
```

> 📝 **注意**：環境變數名稱需要以 `VITE_` 開頭才能在應用程式中被識別。

## 🐛 疑難排解

### 問題：`npm install` 失敗
**解決方案**：
- 清除 npm 快取：`npm cache clean --force`
- 刪除 `node_modules` 資料夾和 `package-lock.json`
- 重新執行 `npm install`

### 問題：開發伺服器無法啟動
**解決方案**：
- 檢查 Node.js 版本：`node -v`
- 確保 5173 埠未被佔用
- 嘗試使用不同的埠：`npm run dev -- --port 3000`

### 問題：模組找不到
**解決方案**：
- 確保已執行 `npm install`
- 重新啟動開發伺服器
- 清除快取：`rm -rf node_modules && npm install`

## 📚 相關資源

- [React 官方文檔](https://react.dev/)
- [Vite 官方文檔](https://vitejs.dev/)
- [Tailwind CSS 官方文檔](https://tailwindcss.com/)
- [React Router 官方文檔](https://reactrouter.com/)

## 📞 需要幫助？

如有任何問題，請：
1. 檢查本文檔以獲得更多資訊
2. 查看專案的 GitHub Issues
3. 聯絡專案維護者

---

祝您使用愉快！🎉
